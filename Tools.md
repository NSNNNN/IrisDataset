برای اینکه **تشخیص بدهیم ابعاد واقعی ویژگی‌ها چندتاست** (یعنی واقعاً چند فیچر برای جداسازی کلاس‌ها لازم داریم)، در یادگیری ماشین چند ابزار و روش استاندارد وجود دارد که دقیقاً برای همین کار طراحی شده‌اند.

بیایید آن‌ها را مرتب و روشن بررسی کنیم.

---

# 🎯 ابزارهای تشخیص تعداد ابعاد واقعی (Feature Dimensionality)

## 1) **PCA – Principal Component Analysis**
### 🔍 بهترین و رایج‌ترین ابزار
PCA به ما می‌گوید:
- چند مؤلفهٔ اصلی بیشترین واریانس داده را توضیح می‌دهند  
- کدام فیچرها اطلاعات تکراری دارند  
- حداقل چند بعد برای نمایش داده کافی است  

### خروجی مهم PCA:
- **Explained Variance Ratio**  
- **Scree Plot** (نمودار زانویی)

در دیتاست Iris معمولاً:
- **۲ مؤلفهٔ اصلی** بیش از ۹۵٪ اطلاعات را پوشش می‌دهند  
- یعنی ابعاد واقعی ≈ ۲

---

## 2) **LDA – Linear Discriminant Analysis**
### 🔥 مخصوص تشخیص ابعاد لازم برای جداسازی کلاس‌ها
LDA برخلاف PCA روی **جدا کردن کلاس‌ها** تمرکز دارد.

در دیتاست‌های چندکلاسه:
- تعداد مؤلفه‌های LDA = تعداد کلاس‌ها − 1  
- در Iris (۳ کلاس) → حداکثر **۲ بعد**

LDA کمک می‌کند بفهمیم:
- آیا فیچرها برای جداسازی کلاس‌ها کافی‌اند  
- کدام ترکیب خطی بهترین تفکیک را می‌دهد  

---

## 3) **Feature Importance** (در مدل‌های درختی)
مدل‌هایی مثل:
- Random Forest  
- Gradient Boosting  
- XGBoost  

می‌توانند اهمیت هر فیچر را بدهند.

اگر فیچری اهمیت نزدیک صفر داشته باشد:
- می‌توان آن را حذف کرد  
- یعنی در ابعاد واقعی نقشی ندارد  

---

## 4) **Correlation Matrix (Heatmap)**
اگر دو فیچر همبستگی خیلی بالا داشته باشند:
- اطلاعات تکراری دارند  
- یکی از آن‌ها را می‌توان حذف کرد  

در Iris:
- petal length و petal width همبستگی بالا دارند  
- sepal width کم‌اهمیت‌تر است  

---

## 5) **Variance Threshold**
اگر فیچری واریانس خیلی کم داشته باشد:
- اطلاعات مفیدی ندارد  
- می‌توان حذفش کرد  

این روش ساده‌ترین روش کاهش بعد است.

---

# 🌿 جمع‌بندی ابزارها

| ابزار | هدف | مناسب برای |
|------|------|-------------|
| **PCA** | پیدا کردن ابعاد واقعی داده | داده‌های بدون برچسب |
| **LDA** | پیدا کردن ابعاد لازم برای جداسازی کلاس‌ها | داده‌های برچسب‌دار |
| **Feature Importance** | تشخیص فیچرهای مهم | مدل‌های درختی |
| **Correlation Matrix** | حذف فیچرهای تکراری | همهٔ داده‌ها |
| **Variance Threshold** | حذف فیچرهای کم‌اطلاعات | داده‌های ساده |

---

# 📌 نتیجه برای دیتاست Iris
- ابعاد واقعی داده ≈ **۲**
- بهترین ابزار برای تشخیص: **PCA + LDA**

---



---


برای تعیین **همبستگی بین ویژگی‌ها (Feature Correlation)** چند ابزار استاندارد و بسیار کاربردی در علم داده و ماشین‌لرنینگ وجود دارد. این ابزارها کمک می‌کنند به تشخیص اینکه کدام فیچرها اطلاعات تکراری دارند و کدام‌ها واقعاً مهم‌اند.

بیایید ابزارها را دقیق و مرتب بررسی کنیم.

---

# 🔍 ابزارهای تعیین همبستگی بین ویژگی‌ها

## 1) **Correlation Matrix (ماتریس همبستگی)**
رایج‌ترین و مهم‌ترین ابزار.

- مقدار همبستگی بین هر دو فیچر را نشان می‌دهد  
- مقادیر بین −1 تا +1  
- +1 یعنی همبستگی کامل  
- 0 یعنی بدون رابطه  
- −1 یعنی رابطه معکوس کامل  

### در پایتون:
```python
df.corr()
```

---

## 2) **Heatmap (نقشه حرارتی همبستگی)**
نمایش گرافیکی ماتریس همبستگی.

- رنگ‌ها شدت همبستگی را نشان می‌دهند  
- سریع‌ترین روش برای تشخیص فیچرهای تکراری  

### با seaborn:
```python
import seaborn as sns
import matplotlib.pyplot as plt

sns.heatmap(df.corr(), annot=True, cmap='coolwarm')
plt.show()
```

---

## 3) **Pairplot (نمودار جفتی)**
برای دیدن رابطهٔ بصری بین فیچرها.

- scatter plot بین هر دو فیچر  
- توزیع هر فیچر روی قطر  

### با seaborn:
```python
sns.pairplot(df)
```

---

## 4) **Pandas `corr()` با روش‌های مختلف**
می‌توانی نوع همبستگی را تغییر بدهی:

- Pearson (پیش‌فرض – برای داده‌های خطی)
- Spearman (برای داده‌های غیرخطی)
- Kendall (برای داده‌های کوچک یا noisy)

مثال:
```python
df.corr(method='spearman')
```

---

## 5) **Feature Importance (در مدل‌های درختی)**
اگرچه مستقیماً همبستگی نیست، اما:

- اگر دو فیچر همبستگی بالا داشته باشند  
- مدل درختی معمولاً یکی را مهم‌تر و دیگری را کم‌اهمیت نشان می‌دهد  

این روش برای تشخیص فیچرهای تکراری مفید است.

---

# 🌿 جمع‌بندی

| ابزار | کاربرد |
|------|---------|
| **Correlation Matrix** | محاسبه عددی همبستگی |
| **Heatmap** | نمایش گرافیکی همبستگی |
| **Pairplot** | مشاهده رابطه بصری بین فیچرها |
| **corr(method=...)** | بررسی انواع همبستگی |
| **Feature Importance** | تشخیص فیچرهای تکراری یا کم‌اهمیت |

---

